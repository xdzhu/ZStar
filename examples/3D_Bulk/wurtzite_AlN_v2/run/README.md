# Generated run directory

`./run.sh prepare` creates `run/production/` here. Only generated clean inputs,
stage markers and local calculator outputs belong under `run/`; archived,
reviewed results stay in `results/`. Do not commit scratch, caches or large
ABACUS/PYATB output trees.
