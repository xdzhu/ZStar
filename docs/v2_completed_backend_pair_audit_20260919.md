# 已完成三维 PBE 双后端响应比较：2026-09-19

AlN、GaN、ZnO、四方 PTO 的 ABACUS 中心应变完整 e/C/d 已收集。
ZnO 的直接 e/C/d 已完成，但辅助内部位移 Lambda 的对称性检查仍未通过；
不能将直接张量结果扩大为全部内部响应均已验收。
本报告比较冻结的完整张量，不新增第一性原理计算，不修改原生 VASP 验收门。
AlN、GaN 及精确六方 ZnO 的对称性开启原生 VASP e/C/d 已验收；PTO 本轮重算
完成前仍保留旧档案的诊断标签。GaN/ZnO 的内部应变分解警告不被删除，但它不再
被错误用作总 e 与 relaxed-ion C 的代数转换阻断条件。

## d33 单独对照

| 三维单晶相 | ABACUS d33 (pm/V) | VASP d33 (pm/V) | (AB−V)/abs(V) | VASP d 状态 |
|---|---:|---:|---:|---|
| AlN，纤锌矿 | 4.975410 | 5.323688 | −6.54% | `ISYM=2` 已验收 |
| GaN，纤锌矿 | 1.872239 | 1.583054 | +18.27% | `ISYM=2` 已验收；分解警告保留 |
| ZnO，纤锌矿 | 9.666372 | 9.750071 | −0.86% | 精确六方 `ISYM=2` 已验收；分解警告保留 |
| PTO，四方 P4mm | 49.084674 | 63.829763 | −23.10% | 诊断；来源 d=null |

pm/V 等价 pC/N。采用完整矩阵 d=1000 e (C[GPa])^−1，
不是 e33/C33。坐标、极性畴和 ZnO 显式旋转沿用冻结比较档案，未择优翻转符号。
两后端参考几何、赝势/基组不同；本轮 ABACUS 工程应变为 ±0.5%，
原生 GaN/ZnO/PTO 为 ±1%。因此这些差值不是单独的软件误差或统计不确定度。
AlN 复用此前验收档案，不将其扰动设置假定为其他原生任务的设置。

## 完整张量，而非仅 d33

以下为 ABACUS−VASP 的最大绝对分量差和 Frobenius 相对差；
分母是完整 VASP 张量的 Frobenius 范数。GaN/ZnO 使用本轮已验收原生 d；
PTO 仍是旧档案诊断值，待当前重算替换。

| 材料 | max Δe (C/m²) | e 范数差 | max ΔC (GPa) | C 范数差 | max Δd (pm/V) | d 范数差 |
|---|---:|---:|---:|---:|---:|---:|
| AlN | 0.073544 | 4.47% | 3.103994 | 1.32% | 0.348986 | 5.98% |
| GaN | 0.102209 | 21.64% | 15.664127 | 4.03% | 0.394840 | 21.23% |
| ZnO | 0.015393 | 2.16% | 11.140767 | 4.43% | 1.614249 | 11.85% |
| PTO | 0.323411 | 6.86% | 19.599344 | 9.86% | 14.745089 | 14.86% |

ZnO 的 d33 接近，不意味着完整 d 接近：完整 d 范数差仍为 11.79%。
PTO 具有显著压电响应，但目前不能声称两后端的 d33 已严格一致。

## 同源内应变路线敏感度

固定相同投影后的 Φ/Z，对比 VASP displaced-atoms 与 strained-cells 的内应变路线。
这是同源敏感度，不是独立参照；不能覆盖 Φ/Z 的共同误差。

| 材料 | max Δe (C/m²) | max ΔC (GPa) | max Δd (pm/V) | 完整 d 范数差 |
|---|---:|---:|---:|---:|
| AlN | 0.005287 | 0.445235 | 0.030962 | 0.51% |
| GaN | 0.007007 | 0.417121 | 0.023358 | 0.82% |
| ZnO | 0.006122 | 0.406023 | 0.065175 | 0.43% |
| PTO | 0.037967 | 1.005349 | 3.873286 | 3.89% |

同源路线差异小于多数跨后端差异，提示不能把跨后端差异全部归因于原始内应变平移残差。
这不是因果定位或放宽验收门的证明。PTO 的完整 d 路线敏感度也不能仅由 d33 的接近替代。
既有投影重建与原生序列化结果接近：四体系 max Δe≤6.98e−6 C/m²、
max ΔC≤8.51e−5 GPa、max Δd≤2.55e−4 pm/V；该检查只证明收集/重建一致性。

## 可复现与下一步

```powershell
$env:PYTHONPATH='D:/Work/Code/zstar-v2-development'
python tools/audit_v2_completed_backend_pairs.py --archive outputs/pbe_database_comparison_20260918 --output outputs/pbe_database_comparison_20260918/completed_backend_pair_audit_NEW.json
python -m pytest tests/test_v2_completed_backend_pairs.py -q
```

输出采用独占新路径，包含完整有符号差矩阵、单位、来源 SHA256、算法 SHA256、版本和来源警告。
冻结结果为 `outputs/pbe_database_comparison_20260918/completed_backend_pair_audit_20260919.json`。
此次数值档案生成后新增的是旋转测试，算法文件未变；复核输入哈希后可重现。
描述性分量阈值沿用精度协议，但不是跨后端验收阈值；标准不确定度保持 null。

优先完成已运行的 PZT/ABACUS 和 PZT/VASP 精确对称性对照，收齐实际几何及计时；
随后用当前完整响应结果完成 PTO 原生 d 的同一验收规则，而非无依据提高精度或重新扫描。
PTO 外部同泛函 d33 参考尚缺，不能用本报告诊断值冒充文献参考。
文献对照、跨档案 C/d 限制见
[PBE 数据库比较](v2_pbe_database_comparison_20260918.md)和
[跨档案参考审计](v2_pbe_cross_archive_d_reference_20260919.md)。
本轮新增 DFT 提交与计算资源消耗均为零；未修改核心 API/CLI、main、v1 正式论文或发布。

本地新增七项审计测试通过，含通用三维旋转下应力对偶 d 的闭合与错误剪切转换负例；
完整回归 645 passed、4345 warnings、32.12 s。警告主要来自已有依赖的弃用接口，
不将当前环境通过当作最低依赖版本的发布验证。
